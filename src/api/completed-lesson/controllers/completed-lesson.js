'use strict';

/**
 * completed-lesson controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::completed-lesson.completed-lesson');
